# Default values for node-bootstrap.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

image:
  repository: docker.io/alpine
  pullPolicy: IfNotPresent
  tag: 3.13

imagePullSecrets: []

nodeSelector: {}

tolerations: []

bootstrapScript: |
  #!/usr/bin/env bash
  echo "Hello World"
