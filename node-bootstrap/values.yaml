# Default values for node-bootstrap.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

image:
  repository: ubuntu
  pullPolicy: IfNotPresent
  tag: 20.04

imagePullSecrets: []

nodeSelector: {}

tolerations: []

runEntrypointScriptChrooted: false

entrypointScript: |
  #!/usr/bin/env bash
  echo "Hello World"